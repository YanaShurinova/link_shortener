apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "shift_course.fullname" . }}-test-connection"
  labels:
    {{- include "shift_course.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "shift_course.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
